import styled from "styled-components";
import ResponsiveContainer from "./ResponsiveContainer";

const Notifications = () => {
  return <ResponsiveContainer>Notifications</ResponsiveContainer>;
};

export default Notifications;
