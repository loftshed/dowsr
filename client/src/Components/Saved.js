import styled from "styled-components";
import ResponsiveContainer from "./ResponsiveContainer";

const Saved = () => {
  return <ResponsiveContainer>Saved</ResponsiveContainer>;
};

export default Saved;
